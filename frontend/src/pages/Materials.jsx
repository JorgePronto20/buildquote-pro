import { useEffect, useState } from 'react'
import Button from '../components/UI/Button'
import Input from '../components/UI/Input'
import Select from '../components/UI/Select'
import Modal from '../components/UI/Modal'
import EmptyState from '../components/UI/EmptyState'
import { materialsApi } from '../api/materials'
import { PROFESSIONS } from '../utils/constants'
import { formatMoney } from '../utils/money'

const empty = { code: '', description: '', category: '', profession: 'electrician', unit: 'un', unit_cost: 0, unit_price: 0, vat_rate: 23, supplier_name: '', brand: '', notes: '' }

export default function Materials() {
  const [materials, setMaterials] = useState([]); const [filters, setFilters] = useState({ category: '', profession: '', q: '' }); const [modal, setModal] = useState(false); const [editing, setEditing] = useState(null); const [form, setForm] = useState(empty); const [error, setError] = useState('')
  async function load() { try { const data = await materialsApi.list(filters); setMaterials(data.materials || []) } catch (err) { setError(err.message) } }
  useEffect(() => { load() }, [JSON.stringify(filters)])
  function open(material = null) { setEditing(material); setForm(material || empty); setModal(true) }
  async function save() { try { editing ? await materialsApi.update(editing.id, form) : await materialsApi.create(form); setModal(false); await load() } catch (err) { alert(err.message) } }
  async function remove(id) { if (!confirm('Remover este material?')) return; await materialsApi.remove(id); await load() }
  const categories = [...new Set(materials.map((m) => m.category).filter(Boolean))]
  return <div className="space-y-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="text-2xl font-extrabold text-gray-900">Materiais</h2><p className="text-sm text-gray-500">Catálogo com preços, marcas e categorias por profissão.</p></div><Button onClick={() => open()}>Adicionar material</Button></div><div className="grid gap-3 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-3"><Select label="Profissão" value={filters.profession} options={PROFESSIONS} placeholder="Todas" onChange={(e) => setFilters({ ...filters, profession: e.target.value })} /><Select label="Categoria" value={filters.category} options={categories} placeholder="Todas" onChange={(e) => setFilters({ ...filters, category: e.target.value })} /><Input label="Pesquisa" value={filters.q} onChange={(e) => setFilters({ ...filters, q: e.target.value })} placeholder="Código, descrição ou marca" /></div>{error && <div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full divide-y text-sm"><thead className="bg-gray-50"><tr><th className="px-4 py-3 text-left">Código</th><th className="px-4 py-3 text-left">Descrição</th><th className="px-4 py-3 text-left">Categoria</th><th className="px-4 py-3 text-left">Marca</th><th className="px-4 py-3 text-right">Preço</th><th className="px-4 py-3 text-right">Ações</th></tr></thead><tbody className="divide-y">{materials.map((m) => <tr key={m.id} className="hover:bg-blue-50"><td className="px-4 py-3 font-mono text-xs">{m.code}</td><td className="px-4 py-3 font-medium">{m.description}</td><td className="px-4 py-3">{m.category}</td><td className="px-4 py-3">{m.brand}</td><td className="px-4 py-3 text-right font-bold">{formatMoney(m.unit_price)}</td><td className="px-4 py-3"><div className="flex justify-end gap-2"><Button variant="secondary" className="px-3" onClick={() => open(m)}>Editar</Button><Button variant="ghost" className="px-3 text-red-600" onClick={() => remove(m.id)}>Remover</Button></div></td></tr>)}</tbody></table>{!materials.length && <div className="p-6"><EmptyState title="Sem materiais" description="Adicione materiais para usar nos orçamentos." /></div>}</div></div><Modal open={modal} onClose={() => setModal(false)} title={editing ? 'Editar material' : 'Adicionar material'} footer={<div className="flex justify-end gap-2"><Button variant="secondary" onClick={() => setModal(false)}>Cancelar</Button><Button onClick={save}>Guardar</Button></div>}><div className="grid gap-4 md:grid-cols-2"><Input label="Código" value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value })} /><Input label="Descrição" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} /><Input label="Categoria" value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} /><Select label="Profissão" value={form.profession} options={PROFESSIONS} onChange={(e) => setForm({ ...form, profession: e.target.value })} /><Input label="Unidade" value={form.unit} onChange={(e) => setForm({ ...form, unit: e.target.value })} /><Input label="Preço Unit." type="number" step="0.01" value={form.unit_price} onChange={(e) => setForm({ ...form, unit_price: Number(e.target.value), unit_cost: Number(e.target.value) })} /><Input label="IVA %" type="number" value={form.vat_rate} onChange={(e) => setForm({ ...form, vat_rate: Number(e.target.value) })} /><Input label="Fornecedor" value={form.supplier_name || ''} onChange={(e) => setForm({ ...form, supplier_name: e.target.value })} /><Input label="Marca" value={form.brand || ''} onChange={(e) => setForm({ ...form, brand: e.target.value })} /></div></Modal></div>
}
